
/**
 * These tests test renjin's ability to execute R-language packages. 
 * 
 * <p>To add another package to test, run install.packages() from an R-2.x installation 
 * and then copy the installed package's folder from $R_HOME/library to the src/test/resources/r/library folder.
 */
package r.packages;