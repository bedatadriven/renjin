# This is a test of the R-to-java compiler
# When evaluated, this script add several values and functions to the
# environment into which it is evaluated

a <- 42

myfunc <- function(x) {
    x*3 - 1
}
