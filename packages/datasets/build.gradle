
apply from: '../../gradle/package.gradle'

dependencies {
    compile project(':packages:utils')

    compileOnly project(':packages:stats')

    testCompile project(':packages:hamcrest')
}

compileNamespace {
    defaultPackages = [];
}

testPackage {
    defaultPackages = [];
}